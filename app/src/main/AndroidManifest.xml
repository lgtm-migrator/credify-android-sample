<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="one.credify.sdk.sample">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".DemoApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SDKSample"
        android:usesCleartextTraffic="true"
        tools:replace="android:supportsRtl,android:label">
        <activity
            android:name="one.credify.sdk.sample.screen.useCase.UseCaseActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="one.credify.sdk.sample.screen.checkOut.CheckOutActivity"
            android:screenOrientation="portrait"/>
        <activity android:name="one.credify.sdk.sample.screen.myPage.MyPageActivity"
            android:screenOrientation="portrait"/>
        <activity android:name="one.credify.sdk.sample.screen.banner.BannerActivity"
            android:screenOrientation="portrait"/>
    </application>

</manifest>